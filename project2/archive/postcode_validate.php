<?php
// require_once("settings.php"); // DB connection

// Retrieve form values
// $state = $_POST['state'];
// $postcode = $_POST['postcode'];

// Query to validate postcode-state combo
// $query = "SELECT * FROM auspostcodes WHERE pcode = ? AND state = ?";
// $stmt = mysqli_prepare($conn, $query);
// mysqli_stmt_bind_param($stmt, "ss", $postcode, $state);
// mysqli_stmt_execute($stmt);
// $result = mysqli_stmt_get_result($stmt);

// Check if result exists
// if (mysqli_num_rows($result) > 0) {
//    echo "Postcode is valid for selected state.";
    // You could redirect or continue processing here
//} else {
//    echo "❌ Invalid postcode for the selected state.";
    // Optionally redirect back to form or display an error message
//}
//mysqli_close($conn);
?>